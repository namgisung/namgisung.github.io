---
layout: wiki
title: for문
wiki_name: java
parent: java/반복문
order: 2
---

for문은 반복 횟수를 알고 있을 때 적합하다. 구조가 조금 복잡하지만 직관적이라 이해하기 쉽다
```java
for (int i=1;i<=;i++;) {
  System.out.println(“yes”);
}
```
위에 예시는 yes을 5번 출력한 것이다.

**(1) 기본 구조**

for문의 구조는 다음과 같다
```java
for(초기화;조건식;증감식;) {
  //실행문
}
```
**(2) 동작 순서**

조건문이 참인 동안 {}내의 문장들을 반복하다 거짓이 되면 반복문을 벗어난다.

제일 먼저 초기화가 수행되고, 그 이후부터는 조건식이 참인 동안 조건식->수행될 문장->증감식 순서로 계속 반복된다. 그러다가 조건문이 거짓이 되면 for문 전체를 빠져나가게 된다.

**(1) 초기화**

반복문에 사용될 변수를 초기화하는 부분이며 처음에 한번만 수행된다.보통 변수 하나로 for문을 제어하지만, 둘 이상의 변수가 필요할 때는 콤마’,’를 구분자로 변수를 초기화하면 된다. 단, 두 변수의 타입은 같아야한다

**(2) 조건식**

조건식이 값이 참이면 반복을 계속하고 거짓이면 반복을 중단하고 for문을 벗어난다. for의 뜻이 ‘~하는 동안’이므로 조건식이 ‘참인 동안’ 반복을 계속한다고 생각하면된다.

**(3) 증감식**
```java
i++
i—-
```
i++은 i에 1씩 증가시킨다랑 똑같은 말이다.
```java
//i++ == i= i + 1
```

i—-은 i에 1씩 감소시킨다랑 똑같은 말이다.
```java
//i— == i= i - 1
```
